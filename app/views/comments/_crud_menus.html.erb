<ul class='crud-menu-btn list-inline float-right'>
  <li class="list-inline-item">
    <%= link_to '#', id: "button-edit-#{board.id}" do %>
      <%= icon 'fa', 'pen' %>
    <% end %>
  </li>
  <li class="list-inline-item">
    <%= link_to comment_path(comment), id: "button-delete-#{board.id}" method: :delete,data: { confirm: t('defaults.message.delete_confirm') }, remote: true do %>
      <%= icon 'fas', 'trash' %>
    <% end %>
  </li>
</ul>